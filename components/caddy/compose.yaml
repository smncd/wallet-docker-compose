---
services:
  caddy:
    image: caddy:latest
    restart: always
    environment:
      - WALLET_CADDY_FRONTEND_FQDN=${WALLET_CADDY_FRONTEND_FQDN:-wallet.localhost}
      - WALLET_CADDY_BACKEND_FQDN=${WALLET_CADDY_BACKEND_FQDN:-backend.localhost}
      - WALLET_CADDY_ISSUER_FQDN=${WALLET_CADDY_ISSUER_FQDN:-issuer.localhost}
      - WALLET_CADDY_VERIFIER_FQDN=${WALLET_CADDY_VERIFIER_FQDN:-verifier.localhost}
      - WALLET_CADDY_CORE_FQDN=${WALLET_CADDY_CORE_FQDN:-core.localhost}
      - WALLET_CADDY_GOBACKEND_FQDN=${WALLET_CADDY_GOBACKEND_FQDN:-gobackend.localhost}
      - WALLET_CADDY_PROTOCOL=${WALLET_CADDY_PROTOCOL:-http://}
    cap_add:
      - NET_ADMIN
    ports:
      - "127.0.0.1:80:80"
      - "127.0.0.1:443:443"
      - "127.0.0.1:443:443/udp"
      - "127.0.0.1:9099:9099" # metrics
    volumes:
      - ./config:/etc/caddy
      - ./registry:/etc/registry
      - caddy_data:/data
      # - caddy_api_config:/config # used for the caddy API
    # develop:
    #   watch:
    #     - path: ./config
    #       action: sync+exec
    #       target: /etc/caddy
    #       exec:
    #         command: caddy reload --config /etc/caddy/Caddyfile

volumes:
  caddy_data:
